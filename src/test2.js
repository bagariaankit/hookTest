function greeting(fName) {
	/* jshint ignore:start */
	//var age = 20;
	/* jshint ignore:end */
	if (fName && fName.length > 0 && age === 20) {
		return "Hello " + fName + "! Welcome to pre-commit world.";
	} else {
		return false;
	}
}
